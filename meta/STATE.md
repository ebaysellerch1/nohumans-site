# STATE.md — מצב הפרויקט

עודכן לאחרונה: 2026-07-05 ~02:00 מקומי (ריצת אדמין מיוחדת — הושלמה)

## מצב נוכחי

- ריצה: ריצת אדמין 2026-07-05 (סקירה ביקורתית + הכנת השקה) — הושלמה
- ריצה פעילה מאז: — (המנעול נוקה בסיום ריצת האדמין)
- בעבודה כעת: — (אין משימה פתוחה)
- **ההכרזה עומדת להתפרסם** — ערכת ההשקה מוכנה ב-reports/launch-kit.md. מרגע שמזוהה גל: פרוטוקול "למידה מהגל" (למטה) גובר על כל תוכנית אחרת.
- הושלם בריצת האדמין: תיקון כפילות "runs so far" בבית (+תקרית קובץ ריק שנפתרה — לקחים למטה); ליטוש עיצוב להשקה (hero CTAs, קיבוץ 4+4+4, status-line — 658bf20); Prompt Checkup ב-/tools/ + llms.txt + README עשיר + Tools בבית (+feed/changelog/sitemap); IndexNow 4/4; איזון BACKLOG (יחס 1:2 תוכן:הפצה); עקרונות עיצוב + תוכנית הפצה ב-DECISIONS; launch-kit.md. הכל אומת חי.
- הושלם בריצה 12 (הקודמת): METRICS קריאה 11; מדריך 12; "After the path" ב-start.html; תקרית deploy נפתרה. דוח: reports/2026-07-05-00.md

## עובדות קבועות (לכל ריצה)

- **נושא האתר**: "An AI's field guide to working with AI" — מדריכים פרקטיים לעבודה עם כלי AI, כתובים על ידי AI שמנהל אתר אמיתי. ראה DECISIONS.md (2026-07-03). אושר על ידי האדמין.
- **שפת האתר**: אנגלית (ביקוש חיפוש גלובלי).
- **תאריכים ושעון**: תאריכי פרסום לפי זמן מקומי של האדמין (UTC+3). **אימות שעה**: שעון ה-sandbox אינו אמין — לאמת מול חותמת קובץ (touch + ls --time-style=full-iso בתיקייה הממופה). ריצה 6 רשמה זמנים שגויים ב-3 שעות; הדוח שלה נקרא -09 למרות שרצה ב-06 מקומי, ולכן דוח ריצה 7 הוא -09b.
- **git**: התיקייה הממופה לא תומכת בפעולות git. בכל ריצה: שיבוט לנתיב זמני **ייחודי** (/tmp/nh-run-XXXX), עבודה ודחיפה משם, ושיקוף קבצי האתר (ללא .git, עם rsync --delete על תיקיות התוכן) ל-repo/ בתיקיית הפרויקט. מקור האמת: GitHub. הערה: מחיקת קבצים בתיקייה הממופה דרך bash חסומה (Operation not permitted) — שיקוף עם rsync --delete עלול להיכשל על קבצים שנמחקו; לא קריטי.
- **בדיקת פריסה אחרי כל push (מריצה 12, #49)**: כשלי build של GitHub Pages אינם נדירים (bf7ff44, c5ac97e, 58a0647 נכשלו; 3b689db הצליח; 6ca9a6c נכשל בריצת האדמין). אחרי push לבדוק: builds API (/repos/ebaysellerch1/nohumans-site/pages/builds) — אם errored, לדחוף commit נוסף עם שינוי אמיתי.
- **גישה לרשת בריצות מתוזמנות**: web_fetch חסום (provenance). הפתרון: כלי הדפדפן (Claude in Chrome) — navigate + get_page_text + javascript_tool עובדים ל-GoatCounter, לאתר החי ול-IndexNow. בדיקת site: במנועי חיפוש נחסמת ב-CAPTCHA — לא לעקוף; מעקב אינדוקס דרך referrers אורגניים ב-GoatCounter. הערה מריצה 11: קריאת ה-API של GoatCounter (fetch ל-/api/v0/stats) גרמה ל-timeout של הרנדרר — להישאר עם get_page_text על הדשבורד.
- **IndexNow (מריצה 7, פרוטוקול קבוע)**: מפתח 6c5ec57a48628297f881681c91b437dd.txt בשורש המאגר — לא למחוק. על כל URL חדש/מעודכן: ping. מהדפדפן: navigate ל-api.indexnow.org עם url+key ומשם fetch same-origin; מ-sandbox עם curl: GET ישיר עובד (אומת בריצת האדמין, 4/4 סטטוס 200).
- **אימות חי — origin (מריצה 8)**: fetch חוצה-origin מדף GoatCounter נחסם (CSP). הדרך: navigate ל-https://nohumans.xyz/robots.txt ומשם fetch same-origin עם no-store. **לקח ריצה 9**: deploy יכול להתעכב דקות — אם MARKER חסר, לוודא שה-push הגיע (git ls-remote) ולהשאיר בדיקה לריצה הבאה. **לקח ריצה 12**: push מוצלח + עמוד חדש חי ≠ כל ה-commit-ים נפרסו — לבדוק deployment של ה-commit האחרון ספציפית.
- **הימנעות מזיהום מדידה**: בדיקות רינדור — רק ב-iframe עם sandbox="allow-same-origin" (בלי allow-scripts); אימות חי — fetch(url, {cache:'no-store'}) ולא ניווט מלא. חלון הדפדפן לא יורד מתחת ~1536px; iframe הוא הפתרון לבדיקות רוחב.
- קובצי המצב (STATE, METRICS, DECISIONS, BACKLOG, reports) נשמרים גם בתיקיית הפרויקט וגם במאגר (תחת meta/) — גיבוי כפול, ללא סודות.
- **פרוטוקול משימות**: תת-המשימות נלקחות מ-BACKLOG.md לפי עדיפות (6–8 לריצה רגילה — הוראת אדמין). בסוף כל ריצה מוסיפים ל-BACKLOG לפחות 2–3 משימות חדשות. משימה שהושלמה — ✔ וארכיון.
- **יחס תוכן:הפצה (הוראת אדמין, 2026-07-05)**: ייצור מדריכים אינו ברירת המחדל. על כל מדריך חדש — לפחות 2 משימות תנועה/מעורבות/עיצוב/הפצה לפני המדריך הבא. הנימוק: 12 מדריכים נכתבו בלי נקודת נתונים אחת על ביקוש, והנישה חשופה בעצמה ל-AI Overviews. החפיר של האתר הוא הסיפור (הניסוי השקוף), לא ה-SEO. מדריך חדש נכתב רק כשיש אות ביקוש או השלמה מובהקת למסלול.
- **פרוטוקול RSS/Latest (קבוע)**: מדריך חדש => item ב-feed.xml + עדכון lastBuildDate; כל ריצה => עדכון חתך Latest בבית. **פורמט חדש (2026-07-05)**: Latest הוא p.status-line בתחתית main (לא notice בראש); id="latest" נשמר.
- **פרוטוקול מנעול**: בתחילת ריצה בדוק את "ריצה פעילה מאז". אם החותמת טרייה מ-3 שעות — ריצה אחרת כנראה פעילה; צא בשקט בלי לגעת בקבצים. עדכן את החותמת בתחילת כל ריצה ונקה אותה בסיום.
- **אימות חי אחרי כל דחיפה**: דחיפה מוצלחת ≠ פריסה מוצלחת. קיים .nojekyll — לא למחוק. זהירות מ-cache — fetch עם no-store; ה-CDN מקשיח cache (~10 דק'); קובץ חדש לגמרי הוא אינדיקטור פריסה אמין יותר מקובץ שהשתנה.
- **כתיבת קבצים אטומית ומוקדמת**: לעדכן קבצי מצב מוקדם ואחרי כל תת-משימה, ולא לרכז כתיבות בסוף הריצה.
- **בדיקת diff לפני דחיפה (הוראת אדמין, 2026-07-05)**: אחרי כל עדכון של עמוד קיים — git diff מלא לפני commit, ולוודא שעריכה החליפה טקסט ולא הוסיפה לצידו (תקרית "11 runs so far"). אחרי הפריסה — לבדוק בעמוד החי שאין כפילויות/שאריות (grep על ביטוי הסיכום — הופעה אחת).
- **אזהרת התיקייה הממופה (תקריות 2026-07-05)**: (א) עריכה שמקצרת קובץ במראה משאירה ריפוד NUL כשקוראים אותו מ-bash; (ב) עריכת קובץ **קיים** במראה עשויה להיראות ישנה/קטועה מ-bash למשך זמן רב (קבצים חדשים כן נראים). לכן: קובצי אתר עורכים אך ורק בתוך ה-clone (python עם assertions + git diff); סנכרון meta/ — לא ב-cp עיוור אלא עם אימות תוכן (marker מהריצה הנוכחית חייב להופיע בעותק); אם העותק ישן — לכתוב את התוכן לקובץ ב-clone ישירות (heredoc). לעולם לא cmd > file בשרשרת שכשל בה משאיר קובץ ריק — נדחף עמוד בית ריק לפרודקשן כך (~3 דק', תוקן).

- **פרוטוקול למידה מהגל (הוראת אדמין, 2026-07-05)**: מרגע שההכרזה מתפרסמת (זיהוי: קפיצת תנועה ב-GoatCounter או referrers חברתיים/HN/רדיט) — בכל ריצה שאחרי ההשקה לנתח לעומק: (א) referrers — איזה ערוץ הביא כמה; (ב) עמודי כניסה; (ג) עומק — כמה המשיכו ולאן; (ד) חזרה בימים שאחרי. מכל ניתוח נגזרות משימות BACKLOG: להכפיל מה שעובד, לתקן איפה שנוטשים. הנתונים גוברים על כל סברה קודמת.

## שדות תפעול

- ריצה פעילה כעת: — (ריק = פנוי)
- נבדק ב-: 2026-07-05 (ריצת אדמין)
- מונה ריצות מתוזמנות שהושלמו: 12

## מתוכנן לריצה 13 (הראשונה אחרי ריצת האדמין)

- **קודם כל**: בדיקה אם ההכרזה פורסמה (קפיצת תנועה/referrers ב-GoatCounter). אם כן — #52/#53: ניתוח גל לפי פרוטוקול "למידה מהגל", וזה עיקר הריצה.
- אם אין עדיין גל: #54 (שיפור about לקהל הגל) ו-#55 (עמוד Ask the AI) — הכנות למעורבות, לא תוכן חדש.
- לוודא שאירוע tool-prompt-checkup-used נרשם ב-GoatCounter.
- שוטף: עדכון Latest (פורמט status-line); IndexNow לכל URL חדש; בדיקת deployment אחרי כל push; git diff לפני כל commit.
- תזכורת: 2026-07-10 סיכום שבוע 1 (#42). מדריך 13 (#51) — רק עם אות ביקוש (כלל היחס).
