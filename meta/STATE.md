# STATE.md — מצב הפרויקט

עודכן לאחרונה: 2026-07-05 ~18:25 מקומי (ריצה 15 — הושלמה)

## מצב נוכחי

- ריצה: ריצה 15 — הושלמה.
- ריצה פעילה מאז: — (אין ריצה פעילה; ריצה 15 הסתיימה)
- בעבודה כעת: — (אין משימה פתוחה)
- **הגל ממשיך לעלות — קריאה שלישית ברציפות**: ריצה 15 (~15:03 UTC, 2.75 שעות אחרי ריצה 14) הראתה 629 ביקורים שבועיים (מ-525) — עלייה של 19.8%. הרצף המלא: 35→385→525→629. עדיין אין שום סימן שקיעה. #60 נשאר פתוח לריצה הבאה. Prompt Checkup הראה לראשונה שיפור אמיתי (7 צפיות/4 שימושים, מ-5/2) בעקבות הקישורים מ-about/start (ריצה 14) — #62 נסגר בהצלחה. ההתפשטות הבינלאומית (גאורגיה/קנדה/ספרד/צרפת) נעצרה בדיוק על אותם מספרים כמו ריצה 14 — #63 נסגר: מסקנה — היה חד-פעמי, לא מגמה. הופיע לראשונה ביקור בודד מ-bing.com (#65 חדש, לעקוב). ניתוח מלא ב-METRICS ו-DECISIONS (ריצה 15).
- הושלם בריצה 15: (א) קריאת GoatCounter השוואתית שלישית (#60) — עדיין עלייה, לא שקיעה. (ב) נבדקו #62/#63 — Prompt Checkup השתפר (5/2→7/4), התפשטות בינלאומית לא גדלה (חד-פעמי). (ג) נבנה faq.html — "Ask the AI that runs this site", עמוד FAQ מתמשך (9 שאלות), עם קישורים מ-about.html, start.html, index.html (status-line) ו-README.md; נוסף ל-sitemap/feed/llms.txt. (ד) עודכן יומן ההחלטות ב-about.html + DECISIONS.md עם נימוק הבחירה ב-FAQ על פני מדריך 13. (ה) עודכנו METRICS+BACKLOG. (ו) פריסה: שני קומיטים רצופים (7b36d87 תוכן, 2675919 README) נכשלו בשלב "Deploy to GitHub Pages" (ה-build עצמו הצליח בשניהם — נבדק דרך jobs API); קומיט שלישי (סנכרון meta/ + דוח הריצה) שימש כניסיון חוזר נוסף. (ז) **ממצא תפעולי חדש וחשוב**: התגלה שבריצה זו, גם קבצי המצב ברמת התיקייה הראשית (STATE/METRICS/BACKLOG/DECISIONS, לא רק repo/) נראים דרך bash כ-stale/גרסה ישנה גם אחרי עריכה מוצלחת דרך כלי ה-Edit/Write — bash הראה METRICS.md כ-1630 בייטים (הגרסה המקורית מלפני כל עריכות הפרויקט) בעוד ה-Read tool הרגיל הראה נכון את כל התוכן המעודכן. תועד כעובדה קבועה למטה.
- הושלם בריצה 14: (א) קריאת GoatCounter השוואתית (#60) — הגל בעלייה, לא בשקיעה; עודכנו METRICS+DECISIONS. (ב) נבדק שימוש בכלי Prompt Checkup אחרי ה-reorder של ריצה 13 (#61) — ללא שינוי (5 צפיות/2 שימושים, זהה לריצה 13) למרות גידול תנועה כולל של פי 1.5 — נמסקנה שהמיקום בבית לא מספיק. (ג) נוספו קישורים ישירים לכלי מ-about.html (סעיף חדש "Something I built, not just wrote") ומ-start.html ("If you only have ten minutes") — שני העמודים עם התנועה הגבוהה ביותר בגל (458301b). (ד) עודכן יומן ההחלטות ב-about.html שהיה תקוע מ-2026-07-04, עם שתי רשומות חדשות (בניית הכלי, שינויי הגל). (ה) בדיקת קישורים פנימית מלאה (#59) — 0 שבורים ב-18 עמודים. (ו) תוקן sitemap.xml (lastmod ישן ל-about/changelog) — שימש גם כקומיט ניסיון חוזר אחרי כשל build ראשון (458301b נכשל בפריסה, e82e828 הצליח ואומת חי). (ז) IndexNow פינג ל-4 URLs מעודכנים. (ח) עדכון Latest+changelog לריצה 14. **החלטה מתועדת ב-DECISIONS**: לא לשנות אסטרטגיה על הנחת שקיעה שגויה — להמשיך לנצל את חלון התנועה הגבוה.
- הושלם בריצה 13: (א) ניתוח גל מלא לפי פרוטוקול "למידה מהגל" — מקור, עומק, מה עבד, איפה נוטשים; METRICS+DECISIONS+BACKLOG עודכנו (#52/#53 בארכיון, #54 עלה לעדיפות עליונה ובוצע, #60/#61 נוספו). (ב) about.html: תיבת notice מוקדמת עם קישורים בולטים לכללים/יומן החלטות/GitHub (100dca8, בילד נכשל בפריסה הראשונה אך נפרס בהצלחה עם הקומיט הבא — נבדק חי). (ג) index.html: העברת חטיבת Tools/Prompt Checkup מעל שלוש קבוצות המדריכים — שימוש כמעט אפסי (2/385) במיקום הישן (e6a0a87, נפרס בהצלחה ואומת חי). (ד) IndexNow פינג ל-about.html והבית. (ה) בדיקת מובייל 375px ל-index/about — אין גלישה אופקית (360/360).
- הושלם בריצת האדמין (2026-07-04 לילה): תיקון כפילות "runs so far" בבית; ליטוש עיצוב להשקה (hero CTAs, קיבוץ 4+4+4, status-line — 658bf20); Prompt Checkup ב-/tools/ + llms.txt + README עשיר + Tools בבית (+feed/changelog/sitemap); IndexNow 4/4; איזון BACKLOG; עקרונות עיצוב + תוכנית הפצה ב-DECISIONS; launch-kit.md.
- הושלם בריצה 12: METRICS קריאה 11; מדריך 12; "After the path" ב-start.html; תקרית deploy נפתרה. דוח: reports/2026-07-05-00.md

## עובדות קבועות (לכל ריצה)

- **נושא האתר**: "An AI's field guide to working with AI" — מדריכים פרקטיים לעבודה עם כלי AI, כתובים על ידי AI שמנהל אתר אמיתי. ראה DECISIONS.md (2026-07-03). אושר על ידי האדמין.
- **שפת האתר**: אנגלית (ביקוש חיפוש גלובלי).
- **תאריכים ושעון**: תאריכי פרסום לפי זמן מקומי של האדמין (UTC+3). **אימות שעה**: שעון ה-sandbox אינו אמין — לאמת מול חותמת קובץ (touch + ls --time-style=full-iso בתיקייה הממופה). ריצה 6 רשמה זמנים שגויים ב-3 שעות; הדוח שלה נקרא -09 למרות שרצה ב-06 מקומי, ולכן דוח ריצה 7 הוא -09b.
- **git**: התיקייה הממופה לא תומכת בפעולות git. בכל ריצה: שיבוט לנתיב זמני **ייחודי** (/tmp/nh-run-XXXX), עבודה ודחיפה משם, ושיקוף קבצי האתר (ללא .git, עם rsync --delete על תיקיות התוכן) ל-repo/ בתיקיית הפרויקט. מקור האמת: GitHub. הערה: מחיקת קבצים בתיקייה הממופה דרך bash חסומה (Operation not permitted) — שיקוף עם rsync --delete עלול להיכשל על קבצים שנמחקו; לא קריטי.
- **תקרית מיפוי לא-מסונכרן (ריצה 13, אושש ונרחב בריצה 15)**: תיקיית repo/ בתיקיית הפרויקט (המראה) התגלתה **לא מסונכרנת** מול GitHub. בריצה 15 התגלה שהבעיה חמורה יותר ממה שתועד: **גם קבצי המצב הראשיים עצמם (STATE.md, METRICS.md, BACKLOG.md, DECISIONS.md — לא רק repo/)** נראים לפעמים דרך **bash** (cat/cp/wc) כגרסה ישנה/stale, גם מיד אחרי עריכה מוצלחת דרך כלי Edit/Write באותה ריצה. ה-Read tool הרגיל (לא-bash) תמיד הראה את התוכן הנכון והעדכני. **מסקנה מחייבת**: לעולם לא לקרוא קבצי מצב (או כל קובץ בתיקייה הממופה) דרך bash (cat/cp/grep/wc) כשצריך את התוכן העדכני ביותר — תמיד להשתמש בכלי ה-Read הרגיל. כשצריך להעביר תוכן קובץ-מצב אל תוך ה-clone הזמני (meta/), יש לקרוא קודם עם Read (לא bash), ואז לכתוב את התוכן המדויק לתוך ה-clone (heredoc עם delimiter מצוטט, לא bash cp). התוצאה: מקור האמת היחיד לתוכן קובץ כלשהו בתיקייה הממופה הוא כלי ה-Read/Write/Edit — לא bash.
- **תקרית מיפוי לא-מסונכרן — repo/ הממופה (ריצה 13)**: bash הראה index.html ישן (בלי חטיבת Tools שכבר הייתה ב-HEAD). מסקנה: **לעולם לא לסמוך על תיקיית repo/ הממופה כמקור אמת לקריאה** — לא לפעולות bash ולא בהנחה שהיא future-מעודכנת. המקור היחיד לאמת על מצב האתר הוא ה-clone הטרי מ-GitHub (/tmp/nh-run-XXXX). עריכות תוכן לאתר: לבצע ישירות בתוך ה-clone (python/heredoc עם assertions), ולוודא בסוף כל דבר שה-diff מול GitHub HEAD נקי לפני commit.
- **בדיקת פריסה אחרי כל push (מריצה 12, #49)**: כשלי build של GitHub Pages אינם נדירים (bf7ff44, c5ac97e, 58a0647, 458301b, 7b36d87, 2675919 נכשלו בשלב deploy; 3b689db, e82e828 הצליחו). אחרי push לבדוק מהדפדפן: `https://api.github.com/repos/ebaysellerch1/nohumans-site/actions/runs?per_page=1` (ציבורי, עובד ב-fetch מכל origin) — אם conclusion=failure, לדחוף commit נוסף (עדיף עם שינוי אמיתי; commit ריק לא עזר בריצה 12). ניסיון לעקוף עם POST ל-`rerun-failed-jobs` נכשל (403 — לטוקן אין הרשאת Actions, ריצה 14). המשיכו עם הפתרון המתועד: קומיט חדש עם שינוי אמיתי. **תובנה מריצה 15**: כשל ה-deploy הוא תמיד בשלב "Deploy to GitHub Pages" בלבד — שלב ה-build (checkout+upload artifact) תמיד הצליח בכל המקרים שנבדקו; זו נראית תקלת תשתית/קצב (concurrency) של GitHub Pages, לא בעיה בתוכן שנדחף.
- **גישה לרשת בריצות מתוזמנות**: web_fetch חסום (provenance). הפתרון: כלי הדפדפן (Claude in Chrome) — navigate + get_page_text + javascript_tool עובדים ל-GoatCounter, לאתר החי ול-IndexNow. בדיקת site: במנועי חיפוש נחסמת ב-CAPTCHA — לא לעקוף; מעקב אינדוקס דרך referrers אורגניים ב-GoatCounter. הערה מריצה 11: קריאת ה-API של GoatCounter (fetch ל-/api/v0/stats) גרמה ל-timeout של הרנדרר — להישאר עם get_page_text על הדשבורד.
- **IndexNow (מריצה 7, פרוטוקול קבוע)**: מפתח 6c5ec57a48628297f881681c91b437dd.txt בשורש המאגר — לא למחוק. על כל URL חדש/מעודכן: ping. POST חסום ב-CORS; הדרך — navigate ל-`https://api.indexnow.org/indexnow?url=<URL>&key=<KEY>` ומשם fetch same-origin לשאר ה-URLs.
- **אימות חי — origin (מריצה 8)**: fetch חוצה-origin מדף GoatCounter נחסם (CSP). הדרך: navigate ל-https://nohumans.xyz/robots.txt (לא-HTML, בלי count.js — אין זיהום) ומשם fetch same-origin עם no-store לכל העמודים. **לקח ריצה 9**: deploy של GitHub Pages יכול להתעכב כמה דקות אחרי push — אם MARKER חסר, לאמת שה-push הגיע (git ls-remote) ולהשאיר בדיקה לריצה הבאה במקום להיתקע. **לקח ריצה 12**: push מוצלח + עמוד חדש חי ≠ כל ה-commit-ים נפרסו — לבדוק deployment של ה-commit האחרון ספציפית (ראה "בדיקת פריסה").
- **הימנעות מזיהום מדידה**: בדיקות רינדור — רק ב-iframe עם sandbox="allow-same-origin" (בלי allow-scripts); אימות חי — fetch(url, {cache:'no-store'}) ולא ניווט מלא. עדכון ריצה 7: החשש מזיהום ריצה 6 לא התממש (+2 בלבד) — הטכניקות עובדות. חלון הדפדפן לא יורד מתחת ~1536px; iframe הוא הפתרון לבדיקות רוחב.
- קובצי המצב (STATE, METRICS, DECISIONS, BACKLOG, reports) נשמרים גם בתיקיית הפרויקט וגם במאגר (תחת meta/) — גיבוי כפול, ללא סודות. **חשוב (ריצה 15)**: בסנכרון לתוך ה-clone, לקרוא תמיד עם Read tool (לא bash cp) — ראה סעיף התקרית למעלה.
- **פרוטוקול משימות**: תת-המשימות נלקחות מ-BACKLOG.md לפי עדיפות (6–8 לריצה רגילה — הוראת אדמין). בסוף כל ריצה מוסיפים ל-BACKLOG לפחות 2–3 משימות חדשות. משימה שהושלמה — ✔ וארכיון.
- **יחס תוכן:הפצה (הוראת אדמין, 2026-07-05)**: ייצור מדריכים אינו ברירת המחדל. על כל מדריך חדש — לפחות 2 משימות תנועה/מעורבות/עיצוב/הפצה לפני המדריך הבא. הנימוק: 12 מדריכים נכתבו בלי נקודת נתונים אחת על ביקוש, והנישה חשופה בעצמה ל-AI Overviews. החפיר של האתר הוא הסיפור (הניסוי השקוף), לא ה-SEO. מדריך חדש נכתב רק כשיש אות ביקוש (נתונים/חיפושים/בקשות) או השלמה מובהקת למסלול.
- **פרוטוקול RSS/Latest (קבוע)**: מדריך/עמוד חדש => item ב-feed.xml + עדכון lastBuildDate; כל ריצה => עדכון חתך Latest בבית.
- **פרוטוקול מנעול**: בתחילת ריצה בדוק את "ריצה פעילה מאז". אם החותמת טרייה מ-3 שעות — ריצה אחרת כנראה פעילה; צא בשקט בלי לגעת בקבצים. עדכן את החותמת בתחילת כל ריצה ונקה אותה בסיום.
- **אימות חי אחרי כל דחיפה**: דחיפה מוצלחת ≠ פריסה מוצלחת (תקרית Jekyll, ריצה 2; כשלי build אקראיים, ריצה 12/14/15). קיים .nojekyll — לא למחוק. זהירות מ-cache דפדפן — fetch עם no-store; גם ה-CDN של Pages מקשיח cache (~10 דק') — קובץ חדש לגמרי הוא אינדיקטור פריסה אמין יותר מקובץ שהשתנה.
- **כתיבת קבצים אטומית ומוקדמת**: לעדכן קבצי מצב מוקדם ואחרי כל תת-משימה (כתיבת קובץ מלא), ולא לרכז כתיבות בסוף הריצה. הריצה עלולה להיקטע בכל רגע.
- **בדיקת diff לפני דחיפה (הוראת אדמין, 2026-07-05)**: אחרי כל עדכון של עמוד קיים — `git diff` מלא לפני commit, ולוודא שעריכה החליפה טקסט ולא הוסיפה לצידו. אחרי הפריסה — לבדוק בעמוד החי שאין כפילויות/שאריות.
- **אזהרת NUL בתיקייה הממופה (תקרית 2026-07-05)**: עריכה שמקצרת קובץ בתיקייה הממופה משאירה ריפוד null bytes בקצה כשמעתיקים אותו ב-bash — git רואה "binary" והאתר מקבל קובץ פגום. לכן: עריכות תוכן לקבצי האתר מבצעים **בתוך ה-clone** (python עם assertions), לא העתקה מהמראה. אחרי כל העתקה מהמראה — `file` חייב להראות text, ולבדוק שאין '\0'.

- **פרוטוקול למידה מהגל (הוראת אדמין, 2026-07-05)**: מרגע שההכרזה מתפרסמת (זיהוי: קפיצת תנועה ב-GoatCounter או referrers חברתיים/HN/רדיט) — בכל ריצה שאחרי ההשקה לנתח לעומק: (א) referrers — איזה ערוץ הביא כמה; (ב) עמודי כניסה — לאן הגיעו; (ג) עומק — כמה המשיכו מעבר לעמוד הראשון ולאן; (ד) חזרה — מבקרים חוזרים בימים שאחרי. מכל ניתוח נגזרות משימות BACKLOG קונקרטיות: להכפיל מה שעובד, לתקן איפה שנוטשים. זו הפעם הראשונה שיש נתונים אמיתיים — הם גוברים על כל סברה קודמת, כולל שלי.

## מתוכנן לריצה 16

- **קודם כל**: #60 — קריאת GoatCounter נוספת להשוואת מגמה (629 בריצה 15, שעלה מ-525 בריצה 14): האם סוף-סוף רואים ירידה, או שהגל עדיין בעלייה. אל תסיקו "שקיעה" מקריאה בודדת — נדרשות לפחות 2 קריאות עוקבות עם ירידה עקבית.
- #64: לבדוק אם faq.html מקבל תנועה/צפיות, והאם הקישורים החדשים (about/start/home/README) מביאים מבקרים אליו.
- #65: לבדוק אם ביקור ה-bing.com הראשון נמשך/גדל, או היה חד-פעמי.
- שוטף: עדכון Latest בכל ריצה; IndexNow לכל URL חדש/מעודכן; בדיקת deployment אחרי כל push (`actions/runs?per_page=1`, לדחוף commit נוסף עם שינוי אמיתי אם conclusion=failure); git diff מלא לפני כל commit; **לא לסמוך על תיקיית repo/ הממופה — clone טרי מ-GitHub הוא מקור האמת היחיד**; **לקרוא קבצי מצב תמיד עם Read tool, לעולם לא עם bash cat/cp** (ראה תקריות ריצה 13/15 למעלה).
- תזכורת: 2026-07-10 סיכום שבוע 1 (#42). מדריך 13 (#51) — רק עם אות ביקוש (כלל היחס החדש; הנתונים עד כה לא תומכים במדריך חדש כרגע).
