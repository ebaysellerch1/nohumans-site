# דוח ריצה — 2026-07-03 15:00 UTC (ריצה מתוזמנת 2)

## מה נעשה

1. **נתונים ראשונים (GoatCounter)**: 16 צפיות עמוד, ~11 מבקרים ייחודיים ביום ההשקה. 12 מתוך 16 הצפיות בעמוד הבית; רק 4 כניסות למדריכים. 94% תנועה ישירה (אין אורגני עדיין — צפוי). ארה"ב 14, ישראל 2. המסקנה הישימה: הבית לא מזרים פנימה — ולכן רוב הריצה הוקדשה לזה.
2. **נוסח הכרזה סופי** (הוראת אדמין): נבחרה טיוטה 2 הסיפורית כבסיס, שולב הפתיח החזק מטיוטה 1, נוספה סגירה שמנהלת ציפיות ("כולל אם הוא ייכשל"). נשמר ב-reports/announcement-final.md — מוכן לפרסום כלשונו.
3. **מדריך רביעי**: "How to verify AI-generated facts quickly" — משלים את מדריך הטעויות, עונה על ביקוש חיפוש סביב הזיות AI, כתוב מזווית "המקור הראשוני לכשלים של עצמי".
4. **קישוריות פנימית**: בלוק "Read next" עם 2 קישורים בכל אחד מ-4 המדריכים (מעגל סגור), עדכון עמוד הבית וה-sitemap.
5. **JSON-LD**: schema.org Article בכל מדריך, WebSite בעמוד הבית. ולידציית JSON עברה.
6. **עמוד changelog ציבורי** (/changelog.html): יומן ריצות קריא לאנושיים + קישור "What's new" בניווט של כל העמודים. סיבת-חזרה + נכס שקיפות. נוסף לפרוטוקול: עדכון רשומה בכל ריצה.

קומיטים: 7a09eb7 → 4d12c0f (7 קומיטים, דחיפה אחרי כל תת-משימה).

## נתונים

ראה METRICS.md. בסיס להשוואה בריצה הבאה: עמודים/ביקור ~1.45. הערה: המדגם הנוכחי הוא כמעט ודאי האדמין וסביבתו — אין להסיק מסקנות תוכן.

## תקלות ולקחים (תפעולי)

- **תקרית פריסה (נפתרה)**: כל הדחיפות של ריצה זו נכשלו בשקט בבניית GitHub Pages (Jekyll build failure החל מהקומיט הראשון של הריצה — כנראה עיבוד Jekyll של קובצי ה-markdown החדשים ב-meta/). האתר החי המשיך להגיש את גרסת הייסוד. אובחן דרך Actions/Deployments API, תוקן עם קובץ .nojekyll (25df4a7) — האתר סטטי ואינו זקוק לעיבוד. אומת חי: changelog + המדריך החדש + הניווט המעודכן באוויר. **לקח קבוע: אימות חי אחרי כל דחיפה הוא לא אופציונלי — דחיפה מוצלחת ≠ פריסה מוצלחת.**

- /tmp/nohumans-site מהריצה הקודמת נשאר נעול (permission denied) — מעכשיו משבטים לנתיב ייחודי פר-ריצה. תועד ב-STATE.md.
- web_fetch חסום בריצות מתוזמנות (provenance) — GoatCounter והאתר נקראים דרך כלי הדפדפן. תועד ב-STATE.md.
- בדיקת site: בבינג נחסמה ב-CAPTCHA — לא עוקפים. מעקב אינדוקס יתבסס על referrers אורגניים.

## מה הלאה (ריצה 3)

(הקטע שוחזר בריצה 3 — הקובץ המקורי נקטע כאן באמצע כתיבה.)

- P1 מה-BACKLOG: מדריך "Making AI write in your voice", RSS feed, חתך "Latest from the experiment" בעמוד הבית, מעקב עומק גלישה.
