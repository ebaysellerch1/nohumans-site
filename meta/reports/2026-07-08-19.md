# Run 19 — 2026-07-08 (~19:45–20:05 local / ~16:45–17:05 UTC)

## Lock check

- Start-of-run: `.runlock18` existed but lacked a "released" marker. Its timestamp (`2026-07-08T12:15:08Z`) matched exactly what run 18 documented as its own clean completion — treated as an unclean-cleanup leftover, not a concurrent-run signal. No `.runlockNN` newer than that existed. Created `.runlock19` (`16:45:06Z`).
- Pre-push check #1 (before commit `caf9e19`): `git fetch origin main` → `origin/main` matched the expected parent exactly. STATE.md re-read showed no unexpected content. Safe to push.
- Pre-push check #2 (before commit `ce5d128`): same check repeated, again matched exactly.
- **No concurrent-run evidence found at any checkpoint. Clean run.**

## Data (GoatCounter)

- Weekly rolling total: 985 visits (barely moved from 984 at run 18 — expected, since the wave has fully decayed).
- **#71 (baseline stabilization) — still open**: 07-07 full day = 53, the **fourth consecutive identical reading** (matches runs 16–19 exactly), confirming measurement stability. 07-08 partial (~70% elapsed) = 13, projected ~18-19 for the full day — still trending down versus 53. No stable floor yet.
- **#75 (live.html traffic) — conclusion strengthened**: expanding GoatCounter's page list to "show more" (985/985 shown, all 18 URLs/events) confirmed live.html **does not appear at all** — exactly 0 visits this week, not "under 7" as estimated last run. Verified live and correctly deployed via direct fetch (not a deploy bug — genuinely just isn't being clicked).
- **#76 (llms-full.txt tracking) — closed as a methodology limit, not a traffic finding**: also absent from the full page list. GoatCounter measures via a JS snippet that only executes on HTML pages; a `text/plain` file can never run that script, so real-world access (including by AI tools) is structurally invisible to this analytics setup regardless of actual usage.
- **#35 (Bing indexing, deadline today)**: bing.com referrer still exactly 1 cumulative visit, unchanged since run 15. Real re-submission would require a Bing Webmaster Tools account — out of sandbox bounds (Constitution §3, no new account signups). IndexNow pinging already happens every run. Closed as "no further action available with current tools."

## What shipped

1. **live.html updated to run 19** numbers (traffic status line, run count).
2. **#73 — OG image extension**: built `og-prompt-patterns.png`, `og-brief-contractor.png`, `og-faq.png`, `og-changelog.png` (Pillow, 1200×630, matching the visual language established for og-about/og-start in run 17). Wired into the four pages' `og:image`/`og:image:alt` tags.
3. **changelog.html**: new Run 19 entry.
4. **index.html**: Latest status line updated to Run 19.

## Deploy verification

- Commit `caf9e19` (live.html + 4 OG images + og:image tag updates): GitHub Actions run `28960331451`, conclusion=success, first attempt.
- Commit `ce5d128` (changelog + index Latest): also deployed successfully, first attempt.
- Live fetch verification (no-store): all touched URLs returned 200 with byte sizes matching local files exactly. Confirmed no duplication — `id="latest"` appears once on the homepage, exactly one "Run 19" changelog heading (Run 18's heading preserved, not overwritten), live.html's "Current run" cell reads 19.
- IndexNow pinged for 6 URLs (live.html + changelog/home/faq/prompt-patterns/brief-contractor) — all 200.
- 0 broken internal links (automated check across 20 HTML files including templates/).

## Backlog changes

- Closed: #76 (methodological limit), #73 (OG extension shipped — moved to "watch for signal" as new item).
- Strengthened: #75 (0 visits confirmed, not "<7"); new #78 opened as its natural next-decision point.
- New items: #78 (decide live.html's fate after 1-2 more runs), #79 (opengraph.xyz preview check for all 6 OG images, never verified externally), #80 (need an indirect CTR-measurement approach before investing in more OG images, since there's no real A/B mechanism here).
- #35 closed as "no further action available."

## Requests for admin

- Same standing request from run 17: let this run know when a Hebrew share is planned, so `/he-share.html` (#67, methodology set in run 18) can be built just ahead of it rather than sitting unused.
- Minor operational note: `.runlock18` was found un-released at the start of this run despite run 18's STATE.md claiming a clean finish — worth keeping an eye on whether lock cleanup is actually executing at end-of-run, not just documented as intended. No indication this caused any problem this time, just flagging the pattern.
- No sign of a concurrent run this time — the double-lock-check protocol (added after runs 16/17) held up cleanly across three checkpoints.
