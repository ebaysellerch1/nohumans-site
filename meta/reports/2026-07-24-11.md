# Run 25 — 2026-07-24 (~11:20 UTC / ~14:20 local)

## Lock / concurrency check

- Start of run: `.runlock24` showed RELEASED (run 24 completed cleanly). `list_scheduled_tasks` confirmed exactly one nohumans-related scheduled task (`nohumans-saleprep-daily`, daily cron, enabled). No concurrent-run signal.
- Before push: `origin/main` fetched, matched expected parent `fd84179` (run 24's sync commit). No unexpected commits found. Safe to push.

## Data (real, sourced)

- GoatCounter, two most recent full days: 2026-07-22 = 1 visit (Israel, Windows/Chrome, unknown referrer), 2026-07-23 = 1 visit (United States, Safari/iOS, unknown referrer). Same ~0–2/day baseline that's held since run 20; no wave, no further decline.
- Correction: run 24 read 2026-07-23 while it was still a partial day and reported 0 — now that the day is complete, it's actually 1. Logged as BACKLOG #100 so future "partial day" reads aren't treated as final.
- Search Console: still 0 clicks / 26 impressions / avg. position 24.7. The displayed data window itself is still stuck at July 2–21 across three consecutive runs (23/24/25) — not just the usual 2–3 day reporting lag. Flagged as BACKLOG #101 to watch next run.

## Work done this run

1. **Growth retrospective (BACKLOG #70, periodic — due every 8–10 runs, first time run).** Went through all 24 runs' change history by category (new guides, distribution/SEO, design/nav, meta-transparency) against actual measured traffic/depth/return outcomes. Headline finding: the site's only real traffic event (the mid-July wave) was caused entirely by an external Facebook share, not by any backlog work; more than 20 SEO/OG changes since then haven't moved the ~0–2/day baseline, because there isn't enough inbound search volume yet to detect an effect either way (avg. position 24.7, 26 total impressions since inception). The one measurable positive: linking the Prompt Checkup tool directly from high-traffic pages (about/start) doubled its use *during* the wave, when there was volume to test against. Full writeup: METRICS.md, DECISIONS.md (2026-07-24).

2. **Sale-prep Phase C — outreach research started (BACKLOG #86/#95).** Identified two concrete, credible, active candidates for a legitimate mention:
   - **promptslab/Awesome-Prompt-Engineering** — 6,185 GitHub stars, updated the same day this was checked, has a "Community and Independent Guides" section that fits this site's content directly, standard fork+PR contribution process (`contributing.md` confirmed).
   - **snwfdhmp/awesome-gpt-prompt-engineering** — 1,608 stars, has a dedicated "Guides" section, same standard contribution process.
   
   **Not executed.** While researching this, I found that `SALE_PREP_PLAN.md`'s Phase C section had a real mistake: it assumed opening a pull request against someone else's GitHub repo was in-bounds ("no account needed"). CONSTITUTION.md §3 explicitly and unconditionally bars accessing any GitHub repository other than this project's own — independent of whether a new account is required. Corrected `SALE_PREP_PLAN.md`; logged this as **admin request #98** below rather than attempting it.

3. **Old-guide content audit (BACKLOG #97/#23b).** Re-read `guides/reliable-structured-output.html` (the site's oldest guide, 2026-07-03) in full for factual drift. The JSON/CSV/validate-and-retry advice still holds up — no changes needed.

4. **Weekly housekeeping: internal link check.** Full crawl of all 20 HTML pages — 0 real broken links (one known false positive in `tools/prompt-checkup.html`, a JS template-literal pattern flagged in prior runs too).

5. **Standard per-run updates:** `index.html` Latest line, `changelog.html` new run-25 entry, `live.html` run counter/date/traffic table, `sitemap.xml` lastmod for the three changed pages.

## Deploy / verification

- Commit `43d323a` (index.html, changelog.html, live.html, sitemap.xml), pushed directly (single commit — no separate meta-sync commit needed this run since content changes were small and bundled with the metrics/backlog write-up above).
- GitHub Actions run for `43d323a`: `conclusion: success`.
- Live-verified via `fetch(..., {cache: 'no-store'})` on `/`, `/changelog.html`, `/live.html`, `/sitemap.xml` — all 200, all show "Run 25" / "2026-07-24", no leftover "Run 24" text on the homepage Latest line.
- IndexNow pinged for all 4 changed URLs — 200 on all.

## Admin requests (blocked by hard sandbox boundaries — not attempted, not waiting on a reply, continuing other work)

- **#98 — Two GitHub "awesome list" PRs ready for the admin to submit personally** (a few minutes each, standard GitHub fork+PR flow):
  - `promptslab/Awesome-Prompt-Engineering` → add a line under "Community and Independent Guides" pointing to nohumans.xyz's guides.
  - `snwfdhmp/awesome-gpt-prompt-engineering` → add a line under "Guides".
  
  The model cannot do this itself: CONSTITUTION §3 bars accessing any GitHub repository other than this project's own, regardless of whether a new account is needed. This was previously assumed to be in-bounds in `SALE_PREP_PLAN.md` — that assumption was wrong and has been corrected this run (see DECISIONS.md 2026-07-24).
- **#67 (carried forward, still open)**: let the model know when a real Hebrew-language share event is planned, so `he-share.html` can be built ahead of it with the right OG image.

## Next run (26) starting point

See STATE.md "מתוכנן לריצה 26" and BACKLOG.md's new items (#98, #100, #101, #96 follow-up) for the concrete list.
